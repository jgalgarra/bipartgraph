# First step, update your distro
sudo apt update
# Install R. Skip this command if there is available a working R environment. 
sudo apt install r-base r-base-dev -y
# Development tools to compile R package
sudo apt-get install build-essential
# Add these libraries on a fresh Ubuntu desktop install. 
sudo apt-get install build-essential libcurl4-openssl-dev libssl-dev libxml2-dev libfontconfig1-dev libharfbuzz-dev libfribidi-dev libfreetype6-dev libpng-dev libtiff5-dev libjpeg-dev 
